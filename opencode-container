#!/bin/bash

set -euo pipefail
trap 'exit 1' INT

BUILD=0
IMAGE_NAME="localhost/claude-container_claude-auth-workspace"
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

usage() {
  cat <<EOF
Usage: $(basename "$0") [OPTIONS] <directory>

Launch a containerized Claude workspace in the given directory.

Options:
  -b, --build    Force a rebuild of the container image
  -h, --help     Show this help message and exit

Files:
  .llmignore  Patterns of files/directories to exclude from the container
                 (format similar to .gitignore)
EOF
}

# Generate volume mounts from .llmignore, computing minimal spanning set
generate_volume_mounts() {
  local work_dir=$1
  local llmignore="$work_dir/.llmignore"

  if ! command -v fdfind >/dev/null 2>&1; then
    echo "Error: fdfind is required" >&2
    return 1
  fi

  if [[ ! -d "$work_dir" ]]; then
    echo "Error: not a directory: $work_dir" >&2
    return 1
  fi

  if [[ ! -f "$llmignore" ]]; then
    echo "Error: .llmignore not found: $llmignore" >&2
    return 1
  fi

  WORK_DIR="$work_dir" CLAUDEIGNORE="$llmignore" python3 "${SCRIPT_DIR}/fd_helper.py"
}

# Generate compose override file with selective volume mounts
generate_compose_override() {
  local work_dir=$1
  local llmignore="$work_dir/.llmignore"

  local override_file="$(mktemp -d)/compose.override.yml"

  # Only generate override if .llmignore exists
  if [[ ! -f "$llmignore" ]]; then
    rm -f "$override_file"
    return
  fi

  # Get minimal mount set
  local mounts_yaml=""
  while IFS= read -r mount_path; do
    if [[ -n "$mount_path" ]]; then
      mounts_yaml+="      - \${CONTEXT}/$mount_path:/workspace/$mount_path"$'\n'
    fi
  done < <(generate_volume_mounts "$work_dir")

  # Always include config mounts
  cat > "$override_file" <<EOF
services:
  claude-auth-workspace:
volumes:
      - ${OPENCODE_CONFIG_DIR:-~}/.opencode:/home/node/.opencode
      - ${OPENCODE_CONFIG_DIR:-~}/.config/opencode:/home/node/.config/opencode
${mounts_yaml}
EOF

  echo "${override_file}"
}

# Parse arguments
POSITIONAL_ARGS=()
while [[ $# -gt 0 ]]; do
  case $1 in
    -b|--build)
      BUILD=1
      shift
      ;;
    -h|--help)
      usage
      exit 0
      ;;
    *)
      POSITIONAL_ARGS+=("$1")
      shift
      ;;
  esac
done

set -- ${POSITIONAL_ARGS[@]+"${POSITIONAL_ARGS[@]}"}

if [[ $# -eq 0 ]]; then
  usage
  exit 1
fi

RUN_DIR="$( cd "$(dirname "$( readlink "$0" || ls -1 "$0" )")" > /dev/null && pwd -P )"
echo "RUN DIR: $RUN_DIR"

WORKING_DIR=$( cd "$1" > /dev/null && pwd )
cd "$WORKING_DIR" || exit 1

touch "${CLAUDE_CONFIG_DIR:-$HOME}/.claude.json"

# Generate compose override with selective mounts if .llmignore exists
OVERRIDE_FILE=$(generate_compose_override "$WORKING_DIR")

# Compose files: base + optional override
COMPOSE_FILES=("-f" "$RUN_DIR/compose.yml" "-f" "$OVERRIDE_FILE")

if [[ $BUILD -eq 1 ]]; then
  CONTEXT="$WORKING_DIR" CLAUDE_CONTAINER_DIR="$RUN_DIR" CLAUDE_CONFIG_DIR="${CLAUDE_CONFIG_DIR:-$HOME}" podman compose "${COMPOSE_FILES[@]}" --in-pod false run --rm --build claude-auth-workspace
else
  CONTEXT="$WORKING_DIR" CLAUDE_CONTAINER_DIR="$RUN_DIR" CLAUDE_CONFIG_DIR="${CLAUDE_CONFIG_DIR:-$HOME}" podman compose "${COMPOSE_FILES[@]}" --in-pod false run --rm claude-auth-workspace
fi

rm $OVERRIDE_FILE
